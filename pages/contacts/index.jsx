import React from 'react';
import withAppLayout from '../../layouts/main';

const Contacts = () => {
  return (
    <div>
      <h1>Контакты</h1>
    </div>
  );
};

export default withAppLayout(Contacts);